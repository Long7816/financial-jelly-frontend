# 前端專案規則

本文件補充專題根目錄的 `AGENTS.md`，適用於 `frontend/` 內的工作。

## 工作範圍

- 預設變更範圍只限本 `frontend/` 目錄及其子目錄。
- 可以唯讀參考專題根目錄的 `AGENTS.md`、`PRD.md` 與相關需求、架構文件；若文件不存在，不得猜測其內容。
- 除非使用者在當前任務中明確授權，不得修改 `frontend/` 以外的檔案，包括後端、Agent、資料庫、原始財報與人工標註資料。

## 跨模組協作

- 如果前端需求需要後端或 API 配合，先說明需要的介面、資料欄位、原因與影響。
- 未取得跨目錄修改授權前，先在前端使用 TypeScript 型別、mock data 或整合規格表達需求，不直接修改其他子專案。
